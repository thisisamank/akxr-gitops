{{- if .Values.zulip.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "zulip.fullname" . }}
  labels:
    {{- include "zulip.labels" . | nindent 4 }}
  {{- with .Values.zulip.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.zulip.ingress.className }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- if .Values.zulip.ingress.tls }}
  tls:
    {{- range .Values.zulip.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.zulip.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "zulip.fullname" $ }}
                port:
                  number: {{ $.Values.zulip.service.port }}
          {{- end }}
    {{- end }}
{{- end }}

