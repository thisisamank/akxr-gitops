apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "zulip.fullname" . }}-rabbitmq
  labels:
    {{- include "zulip.rabbitmq.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  {{- if .Values.rabbitmq.persistence.storageClass }}
  storageClassName: {{ .Values.rabbitmq.persistence.storageClass }}
  {{- end }}
  {{- if .Values.rabbitmq.persistence.volumeName }}
  volumeName: {{ .Values.rabbitmq.persistence.volumeName }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.rabbitmq.persistence.size }}


