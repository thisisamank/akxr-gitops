{{- if .Values.ghost.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "ghost.fullname" . }}
  labels:
    {{- include "ghost.labels" . | nindent 4 }}
  {{- with .Values.ghost.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.ghost.ingress.className }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- if .Values.ghost.ingress.tls }}
  tls:
    {{- range .Values.ghost.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.ghost.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "ghost.fullname" $ }}
                port:
                  number: {{ $.Values.ghost.service.port }}
          {{- end }}
    {{- end }}
{{- end }}

